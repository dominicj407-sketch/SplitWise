server:
  port: 8080
spring:
  application:
    name: group-finance-tracker
  profiles:
    active: postgres
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/group_finance}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:2006}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  h2:
    console:
      enabled: false
  sql:
    init:
      mode: never
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USER:dominicj407@gmail.com}
    password: ${MAIL_PASS:gdms bwxe olto ldyl}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  google:
    client-id: ${GOOGLE_CLIENT_ID:1031620332525-ldlsnod444okmeq5clj6fscs0c8a4eh5.apps.googleusercontent.com}
  frontend-url: http://localhost:5173
